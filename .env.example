# Required before production launch: one approved HTTPS canonical origin.
NEXT_PUBLIC_SITE_URL=https://your-approved-domain.example

# Optional: Google Search Console verification token after ownership is confirmed.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Optional: Cloudflare Worker endpoint for the public aggregate visitor count.
NEXT_PUBLIC_VISITOR_COUNTER_ENDPOINT=
